# 6) Видалити дублікати з відсортованого масиву (повернути k)
def remove_duplicates(nums):
    if not nums: return 0
    write = 1
    for read in range(1, len(nums)):
        if nums[read] != nums[read - 1]:
            nums[write] = nums[read]
            write += 1
    return write
# Приклади:
# nums=[1,1,2]; k=remove_duplicates(nums); print(k, nums[:k])  # 2 [1,2]
# nums=[0,0,1,1,1,2,2,3,3,4]; k=remove_duplicates(nums); print(k, nums[:k])  # 5 [0,1,2,3,4]
