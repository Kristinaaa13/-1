# 5) Об’єднати відсортовані масиви в nums1 (in-place)
def merge(nums1, m, nums2, n):
    i, j, k = m - 1, n - 1, m + n - 1
    while j >= 0:
        if i >= 0 and nums1[i] > nums2[j]:
            nums1[k] = nums1[i]; i -= 1
        else:
            nums1[k] = nums2[j]; j -= 1
        k -= 1
# Приклади:
# x=[1,2,3,0,0,0]; merge(x,3,[2,5,6],3); print(x)  # [1,2,2,3,5,6]
# y=[1]; merge(y,1,[],0); print(y)                  # [1]
# z=[0]; merge(z,0,[1],1); print(z)                 # [1]
